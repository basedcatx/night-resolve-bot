import { test, expect } from 'bun:test';
import { db } from '../database';
import { sql } from 'drizzle-orm';

// test('Verifies database connection', async function exec() {
//   expect(await db.execute(sql`select *`)).toBeTruthy();
// });
